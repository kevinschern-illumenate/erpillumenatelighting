# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for erpillumenatelighting (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-14 (commit 4685f69). Confidence: 97%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how / where / why question. `confidence: "high"` or `grounding: "extracted"` is content-grounded, so cite it directly; `symbol_bodies` carries full live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 741 | **Lines:** 138727 | **Monorepo:** 2 packages | **Import cycles:** 3
erpillumenatelighting is a multi-package python codebase of 741 files, split across 2 packages. Execution starts at tools/fixture_builder/__main__.py, tools/configurator_ui/src/App.jsx, tools/configurator_ui/src/main.jsx and 5 other entry points. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `tools` — The **Configurator UI Build App** is the build-time tooling that powers the configurator’s UI preview artifacts and the YAML-builder UI…
- `illumenate_lighting/illumenate_lighting/doctype/ill_configured_fixture` — **Configured Fixture Mapping** is the portion of the lighting configuration domain that defines how an *ill_configured_fixture* is…
- `illumenate_lighting` — illumenate_lighting · illumenate_lighting/config · illumenate_lighting/illumenate_lighting ·…
- `tools/fixture_builder` — Fixture Builder CLI is the command-line orchestration and configuration boundary for generating fixture-related artifacts (templates…
- `illumenate_lighting/templates/pages` — The **Page Templates** subsystem provides the Python UI-layer page controllers for the portal’s account, collaborators, and configuration…
- `illumenate_lighting/illumenate_lighting/doctype/ill_project` — Project Fixture Scheduling is the part of the illumenate_lighting system that models projects and their fixture schedules, and it ties…
- `tools/configurator_ui/src` — The Configurator UI Source is the React-focused UI/service layer that powers the configurator’s wizard flow and supporting UI widgets under…
- `tools/fixture_builder/generators` — The fixture generator library is the tooling sibling to configuration and higher-level orchestration: it focuses on producing CSV-based…
- `illumenate_lighting/illumenate_lighting/doctype/ill_driver_submittal_mapping` — Driver submittal mapping is the subsystem that takes driver/template/extrusion/fixture template concepts and translates them into the…
- `illumenate_lighting/illumenate_lighting/api` — The Configurators API Layer under illumenate_lighting/illumenate_lighting/api exposes the public, request-driven configuration and…

### Entry points
- `tools/fixture_builder/__main__.py`
- `tools/yaml_builder_ui/src/App.jsx`
- `tools/configurator_ui/src/main.jsx`
- `tools/yaml_builder_ui/src/main.jsx`
- `tools/configurator_ui/src/App.jsx`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `illumenate_lighting/hooks.py` — 7 bug fixes, last fix 5 weeks ago (bug magnet); 10 commits/90d
- `illumenate_lighting/illumenate_lighting/api/spec_submittal.py` — 11 bug fixes, last fix 6 weeks ago (bug magnet); 12 commits/90d
- `illumenate_lighting/illumenate_lighting/api/tape_neon_configurator.py` — 9 bug fixes, last fix 3 weeks ago (bug magnet); 10 commits/90d
- `illumenate_lighting/illumenate_lighting/doctype/ill_webflow_product/ill_webflow_product.py` — 7 bug fixes, last fix 6 weeks ago (bug magnet); 13 commits/90d
- `illumenate_lighting/illumenate_lighting/api/exports.py` — 8 bug fixes, last fix 2 months ago (bug magnet); 2 commits/90d

### Code health
Three co-equal signals: defect risk 7.26/10 avg, hotspot health 2.43/10 (stable), worst `illumenate_lighting/illumenate_lighting/api/portal.py` at 1.0/10 · maintainability 7.7/10 · performance risk 52 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `illumenate_lighting/illumenate_lighting/doctype/ill_webflow_product/ill_webflow_product.py` — change entropy — impact −2.1
- `illumenate_lighting/illumenate_lighting/api/tape_neon_configurator.py` — change entropy — impact −2.1
- `illumenate_lighting/hooks.py` — change entropy — impact −2.1
- `illumenate_lighting/illumenate_lighting/api/webflow_export.py` — change entropy — impact −2.0
- `illumenate_lighting/illumenate_lighting/api/configurator_engine.py` — change entropy — impact −1.8

### Standing decisions (ask `get_why` before diverging)
- Do not add LED Sheet option in category selector

### Commands
- Lint: `ruff check .`
- Format: `ruff format .`

<!-- REPOWISE:END -->
